import { Main, Selector } from '../component/index';

const ROUTES = {
  Main: { screen: Main },
  Selector: { screen: Selector }
};

const ROUTECONFIG = {
  header: null,
}

export { ROUTES, ROUTECONFIG };
